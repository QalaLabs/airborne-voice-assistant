# Core AI Voice Assistant Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
aiofiles>=23.2.1
requests>=2.31.0
python-dotenv>=1.0.0

# OpenAI & AI Services
openai>=1.3.0
tiktoken>=0.5.2

# Voice & Audio Processing
SpeechRecognition>=3.10.0
pydub>=0.25.1
edge-tts>=6.1.10

# Google Cloud Platform (Cloud Storage & Cloud SQL)
google-cloud-storage>=2.14.0

# Vertex AI Agent Engine client (agent_client.py) -- only needed once
# USE_AGENT_ENGINE is enabled. Package name/version UNVERIFIED (see plan's
# "Unverified facts" section) -- confirm against current Agent Engine docs
# before relying on this pin.
google-cloud-aiplatform>=1.60.0

# Telephony
twilio>=8.10.0
piopiy>=0.1.0

# Database & Vectors
psycopg2-binary>=2.9.9
pgvector>=0.2.4
sqlalchemy>=2.0.23

# Scheduling & Utilities
apscheduler>=3.11.3
numpy>=1.24.0